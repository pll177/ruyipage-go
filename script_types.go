package ruyipage

import "ruyipage-go/internal/units"

type (
	// RealmInfo 表示单个 realm 信息。
	RealmInfo = units.RealmInfo
	// ScriptRemoteValue 表示 script 返回的远端值。
	ScriptRemoteValue = units.ScriptRemoteValue
	// ScriptResult 表示 script.evaluate / script.callFunction 的结果。
	ScriptResult = units.ScriptResult
	// PreloadScript 表示预加载脚本标识。
	PreloadScript = units.PreloadScript
)
